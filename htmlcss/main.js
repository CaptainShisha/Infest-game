// import { Continent, Planet, Player, Virus } from './world/index';
    $('#virusNameInput').click('click', () => 
    {
        if($('#virusNameInput').val() === `Enter virus name`){
            $('#virusNameInput').val('');
        }
    });
    $('.smallContinents').click((sender)=> {
       sender.currentTarget.children[0].textContent)
    })

console.log('Ebete si maikata VS');